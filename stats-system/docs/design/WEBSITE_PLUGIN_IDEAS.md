Also implament the SMF itself and add the plugin

1. we need a plugin for the players stats
2. we need a plugin for the servers stats
3. we need a plugin for joining tournements
4.  we need a plugin for joining teams
 5. we need a plugin for STARTING a tournement!
 5. we need a plugin for players to be able to generate their LOGIN token. This login token is what we will use with the "login" registercmd in the tracker.scr. We need ot link that VERY IMPORTANT
 6. Once logged in, We should use the DB ID of the plaeyr from SMF> in the API we should use the ID of the player from SMF (DB)
 7. We want a player dashboard so they can see ALL stats and all THEIR stats .

 
 